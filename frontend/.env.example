# Backend API base URL
# In development, Vite proxy forwards /api to localhost:8000 (leave unset).
# In production on Vercel, this should be empty so relative /api paths are used
# with vercel.json rewrites proxying to the Render backend.
VITE_API_BASE_URL=
